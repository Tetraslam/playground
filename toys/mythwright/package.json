{
  "name": "mythwright",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A world that simulates itself forward: tectonics -> climate -> rivers -> biomes -> civilizations, watched in an animated terminal UI.",
  "scripts": {
    "start": "bun run src/main.ts",
    "smoke": "bun run src/smoke.ts",
    "snapshot": "bun run src/snapshot.ts"
  },
  "dependencies": {
    "@opentui/core": "^0.1.27"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
