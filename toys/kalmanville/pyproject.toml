[project]
name = "kalmanville"
version = "0.1.0"
description = "A tiny city state-estimation toy: noisy transit pings, Kalman filtering, and forecast maps rendered as SVG."
requires-python = ">=3.12"
dependencies = []
